# =============================================================================
#  Laufzeitabhaengigkeiten. Zwei Bloecke: das Spiel und die Asset-Aufbereitung.
#  Die Erzeugungsseite (Blender, siehe tools/blender/) hat ihre eigene Umgebung unter
#  tools/ComfyUI/venv und steht hier bewusst nicht drin.
# =============================================================================

# --- Spiel, uebernommen aus dem 2D-Projekt -----------------------------------
pygame-ce>=2.4.0
pymunk>=6.8.0
numpy>=1.26.0
opencv-python>=4.8.0
# Liest die Motoraufnahmen (src/core/sfx.py, Schichten._laden).
soundfile>=0.12.0
# Audiofaden fuer den Motorklang (src/core/tonausgabe.py). PortAudio liefert
# den Rueckruf, den pygame.mixer nicht hat. Unter Linux muss die
# Systembibliothek vorhanden sein; fehlt sie, faellt das Spiel zurueck.
sounddevice>=0.4.6

# --- 3D-Darstellung ----------------------------------------------------------
# ModernGL rendert die Szene in dasselbe pygame-Fenster. pygame behaelt
# Fenster, Eingabe, Audio, HUD und Menues - die HUD-Flaeche wird als Textur
# darueber gelegt. So bleiben rund 33.000 Zeilen pygame-Code unberuehrt.
moderngl>=5.10.0

# --- Modelle laden -----------------------------------------------------------
# Das Spiel liest die GLB-Modelle zur Laufzeit selbst (src/render3d/mesh.py)
# und braucht pillow fuer die Texturen. trimesh steht in requirements-dev.txt:
# nur die Tests erzeugen damit GLB-Dateien.
#
# scipy steht bewusst NICHT hier, sondern in requirements-dev.txt: es wird nur
# von Werkzeugen unter tools/ gebraucht, also auf dem Entwicklungsrechner.
# Diese Grenze verwischt leicht und wird von
# tests/test_auslieferung.py namentlich bewacht.
pillow>=10.0.0
